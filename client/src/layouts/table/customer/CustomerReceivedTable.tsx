import { TCustomerTableProps } from "./types";

const CustomerReceivedTable = ({
    data,
    pagination,
    setPagination,
    totalRow,
    isError,
    isFetching,
    isLoading,
    refetch,
}: TCustomerTableProps) => {
    return (
        <>
            <div>CustomerReceivedTable</div>
        </>
    );
};

export default CustomerReceivedTable;
