import { TCustomerTableProps } from "./types";

const CustomerUndeliveredTable = ({
    data,
    pagination,
    setPagination,
    totalRow,
    isError,
    isFetching,
    isLoading,
    refetch,
}: TCustomerTableProps) => {
    return (
        <>
            <div>CustomerUndeliveredTable</div>
        </>
    );
};

export default CustomerUndeliveredTable;
