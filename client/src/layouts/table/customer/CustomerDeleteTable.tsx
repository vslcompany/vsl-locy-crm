import { TCustomerTableProps } from "./types";

const CustomerDeleteTable = ({
    data,
    pagination,
    setPagination,
    totalRow,
    isError,
    isFetching,
    isLoading,
    refetch,
}: TCustomerTableProps) => {
    return (
        <>
            <div>CustomerDeleteTable</div>
        </>
    );
};

export default CustomerDeleteTable;
