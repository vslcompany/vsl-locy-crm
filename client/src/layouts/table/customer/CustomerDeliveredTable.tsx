import { TCustomerTableProps } from "./types";

const CustomerDeliveredTable = ({
    data,
    pagination,
    setPagination,
    totalRow,
    isError,
    isFetching,
    isLoading,
    refetch,
}: TCustomerTableProps) => {
    return (
        <>
            <div>CustomerDeliveredTable</div>
        </>
    );
};

export default CustomerDeliveredTable;
